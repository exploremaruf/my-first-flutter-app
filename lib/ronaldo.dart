import 'package:flutter/material.dart';

class ronaldo extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ronaldo',style: TextStyle(
          color: Colors.pink
        ),),
      ),
    );
  }
}
